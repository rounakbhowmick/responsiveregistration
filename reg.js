const form = document.querySelector('.signup-form');
const form = document.querySelector('.signup-form');
const name = do cument.querySelector('#name')
    form.addEventListener('submit', e => {
        e.preventDefault();

    });